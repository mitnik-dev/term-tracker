<?xml version="1.0" encoding="utf-8" ?>
<mct:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
           xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
           xmlns:mct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
           x:Class="TermTracker.Views.Popups.EditTermPopup">
    <Border BackgroundColor="{StaticResource BgLight}"
            Padding="20">
        <VerticalStackLayout Spacing="14" WidthRequest="280">
            <Label Text="Edit Term" Style="{StaticResource SectionHeaderText}"
                   FontSize="18"
                   HorizontalOptions="Center"/>

            <Label Text="Name" Style="{StaticResource DefaultText}" />
            <Entry Text="{Binding Name}" Placeholder="Term name" />

            <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                <VerticalStackLayout Grid.Column="0" Spacing="4">
                    <Label Text="Start Date" Style="{StaticResource DefaultText}" />
                    <DatePicker Date="{Binding StartDate}" />
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Column="1" Spacing="4">
                    <Label Text="End Date" Style="{StaticResource DefaultText}" />
                    <DatePicker Date="{Binding EndDate}" />
                </VerticalStackLayout>
            </Grid>

            <Button Text="Delete Term"
                    BackgroundColor="{StaticResource Danger}"
                    TextColor="White"
                    Clicked="OnDeleteClicked" />   

            <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                <Button Text="Cancel"
                        Clicked="OnCancelClicked"/>
                <Button Text="Save"
                        Clicked="OnSaveClicked"
                        Grid.Column="1"/>
            </Grid>

           
        </VerticalStackLayout>
    </Border>
</mct:Popup>
